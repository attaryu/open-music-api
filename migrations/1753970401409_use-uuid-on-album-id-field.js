/**
 * @param pgm {import('node-pg-migrate').MigrationBuilder}
 * @param run {() => void | undefined}
 * @returns {Promise<void> | void}
 */
exports.up = (pgm) => {
	pgm.alterColumn('albums', 'id', {
		type: 'VARCHAR(36)',
		default: pgm.func('gen_random_uuid()'),
	});

	pgm.alterColumn('songs', 'album_id', { type: 'VARCHAR(36)' });

	pgm.alterColumn('user_albums_like', 'album_id', {
		type: 'VARCHAR(36)',
		notNull: true,
	});
};

/**
 * No down migration, previous ID type was need to be generated by code.
 * Current code expects UUIDs for the 'id' field in 'authentications'.
 */
exports.down = () => {};
