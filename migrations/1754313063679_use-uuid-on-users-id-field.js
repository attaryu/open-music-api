/**
 * @param pgm {import('node-pg-migrate').MigrationBuilder}
 * @param run {() => void | undefined}
 * @returns {Promise<void> | void}
 */
exports.up = (pgm) => {
	pgm.alterColumn('users', 'id', {
		type: 'VARCHAR(36)',
		default: pgm.func('gen_random_uuid()'),
	});

  pgm.alterColumn('authentications', 'user_id', {
    type: 'VARCHAR(36)',
    notNull: true,
  });

  pgm.alterColumn('collaborations', 'user_id', {
    type: 'VARCHAR(36)',
    notNull: true,
  });

  pgm.alterColumn('playlist_song_activities', 'user_id', {
		type: 'VARCHAR(36)',
		notNull: true,
	});

  pgm.alterColumn('user_albums_like', 'user_id', {
		type: 'VARCHAR(36)',
		notNull: true,
	});
};

/**
 * No down migration, previous ID type was need to be generated by code.
 * Current code expects the id generated by the database.
 */
exports.down = () => {};
